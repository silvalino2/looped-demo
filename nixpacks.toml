[phases.setup]
nixPkgs = ["nodejs_18", "python3", "ffmpeg", "unzip", "curl"]

[phases.install]
cmds = ["npm run install:all"]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "cd server && node server.js"
